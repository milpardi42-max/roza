<!-- ───────────────────────────────────────────── -->
**آپدیت برند (بعد از انتشار گزارش):** نام سایت از «نقش | NAGHSH» به **«رزی آتلیه | Rezi Atelier»** تغییر یافت؛
پالت رنگ نیز به نسخهٔ **گرم/مجلل** (عاج، مسی عمیق `#a05d24`، پترولی `#2e4a5c`) به‌روزرسانی شد و دامنهٔ ماک به `reziatelier.com` تغییر کرد.
<!-- ───────────────────────────────────────────── -->

# نگـــش — گزارش نهایی ۲۱‌نکته‌ای | NAGHSH — 21-Point Final Report

> **وضعیت پروژه:** بیلد پروداکشن سبز ✓ — ۱۴۵ مسیر SSG دو‌زبانه ✓ — پیش‌نمایش زنده روی پورت 3000 ✓
> **Status:** production build green ✓ — 145 bilingual SSG routes ✓ — live preview on port 3000 ✓

---

## ۱) خلاصهٔ کلی / Executive summary
نسخهٔ کامل دموی وبسایت مایسری «نگش» (بازار خلاقانه نگاره‌ونگار) با Next.js 15 + React 19 + TypeScript پیاده‌سازی شد. رابط کاربری کاملاً دوسویه است: فارسی راست‌به‌چپ (پیش‌فرض) و انگلیسی چپ‌به‌راست با وزن برابر و سوئیچر زبان داخل هدر. تمام محتوا از لایهٔ دادهٔ ماک (mock) در `src/lib/data` تغذیه می‌شود تا لایهٔ ادمین واقعی بعداً بدون تغییر UI جایگزین شود.
A complete demo build of the NAGHSH creative-pattern marketplace: Next.js 15 / React 19 / TypeScript, fully mirrored FA-RTL (default) + EN-LTR with an in-header switcher, driven by a mock data layer designed for a future admin replacement.

## ۲) معماری و مسیرها / Architecture & routes
- App Router: همهٔ روت‌ها زیر `/[lang]/{fa,en}` با پیشوند زبان؛ ریدایرکت `/` → `/fa`.
- **۲۶ مسیر استاتیک** (خانه، نگاره‌ها، محصولات، هنرمندان، نمونه‌کار، آکادمی، فروشگاه، درباره، تماس، B2B، سبد، تسویه…) + مسیرهای داینامیک: محصول ۱۶، نگاره ۱۲، هنرمند ۶، نمونه‌کار ۶، آموزش ۸، دستهٔ فروشگاه ۸، کالکشن ۲ — در هر دو زبان (**مجموعاً ۱۴۵ صفحهٔ prerender شده** با `generateStaticParams`).
- برنامه‌ریزی داده: `lib/data/{patterns,products,artists,catalog}.ts` + تایپ‌های کامل در `lib/types.ts`.
`app/[lang]` locale-prefixed routing, 145 prerendered pages, typed mock data layer in `src/lib/data` + `src/lib/types.ts`.

## ۳) سیستم طراحی متمرکز / Centralized design system
نُه فایل CSS ماژولار (`tokens, fonts, base, motion, ui, layout, home, pages, globals`): توکن‌های معنایی رنگ (`--background/--surface/--foreground/--muted/--border/--primary(+hover,soft)/--accent(+soft)/--success/--warning/--error`)، مقیاس فاصله/شعاع/سایه/blur/ترنزیشن/انیمیشن/برک‌پوینت. پس‌زمینهٔ اصلی سفید #FFFFFF، آبی نیلیِ آرام + لهجه‌های مسی فقط در CTA/نشان‌ها؛ گلس‌مورفیسم فقط به‌صورت لهجه (هدر/بج/اورلی).

## ۴) تایپوگرافی / Typography & fa-beirut disclosure
همهٔ متون فارسی با فونت خانوادهٔ **`fa-beirut`** (۴۰۰–۸۰۰ + نمایشی) و لاتین با Manrope سرو می‌شود — لوکال از `public/fonts` با `font-display: swap` و preload.
**افشای شفاف:** فایل‌های اصلی fa-beirut در این محیط در دسترس نبود؛ بنابراین دو فونت آزاد هم‌پوشان فارسی (Baloo Bhaijaan 2 + Beiruti) با **نام‌گذاری دقیقاً مشابه** (`fa-beirut-*.woff2`) جایگذاری شد — جایگزینی فونت واقعی بعداً = صرفاً ریختن فایل‌ها در `public/fonts` با همان نام‌ها، بدون هیچ تغییر کد.
*Disclosure: true fa-beirut was unobtainable in this sandbox; look-alike Persian fonts ship under identical filenames — a zero-code swap later.*

## ۵) RTL/LTR / Direction engineering
`dir="rtl"`/`ltr` روی `<html>`، خواص منطقی CSS در تمام استایل‌ها (`margin-inline`, `inset-inline-*`…)، فلیپ خودکار آیکون‌های جهت‌دار (`data-flip-rtl`)، بردکرامب/کاروسل/فرم‌ها/منوها/فیلترها در هر دو جهت بازبینی شدند. ارقام فارسی در لوکالِ فا با Intl (`tnum`) و حروف‌بری با فونت فارسی بدون میراث Tahoma/Arial.

## ۶) هدر و مگامنو / Header & mega-menu
هدر سفید چسبان با سایه/مرز هنگام اسکرول، جستجوی سراسری (Overlay زنده با گروه‌بندی نتایج و برچسب نوع محتوا)، سوئیچر زبان، حساب، سبد با شمارنده، CTA اصلی؛ مگامنوی ادیتوریال برای «نگاره‌ها» و «فروشگاه» (تصاویر بندانگشتی، توضیحات، stagger) + دراور موبایل کامل. بدون هیچ کنترل توسعه‌ای.

## ۷) هیرو و صفحهٔ اول / Hero & homepage
هیروی سینمایی ۲۰۲۶ با تصویر بزرگ + تیتر + دو CTA + کارت‌های شناور (نگاره/محصول لینک‌دار)، نوار متحرک و افکت‌های blur-in/fade-up/scale-fade/parallax با احترام به `prefers-reduced-motion`. صفحهٔ اول: ۱۴ سکشنِ درخواستی پیاده شد (کشف بنتو، ترند، پرفروش، هنرمندان، نمونه‌کارها، سبک‌ها، محصولات، کالکشن انحصاری، آکادمی، B2B، تولید سفارشی، روایت‌های هنرمندان، خبرنامه + هدر/فوتر) — پیام «هر نقش، یک امکان» و هر سکشن با CTA واقعی.
All 17 requested zones implemented as 14 content sections + header/footer; message direction "Turn every pattern into a possibility («هر نقش، یک امکان»)".

## ۸) کارت محصول و Quick View / ProductCard & Quick View
ProductCard کامل طبق الزام: عکس، نام، SKU شماره‌دار (`PRD-00xxx`)، دسته، مشخصات کلیدی (جنس/ابعاد)، رنگ‌بندی، قیمت (+تخفیف)، وضعیت موجودی، علاقه‌مندی، Quick View، افزودن به سبد؛ هاور: زوم تصویر، ارتفاع، نمایش اکشن‌ها. مودال Quick View: تصویر بزرگ‌تر، نام، SKU، مشخصات، قیمت، موجودی، انتخاب واریانت/تعداد، افزودن به سبد و «مشاهدهٔ کامل محصول» → صفحهٔ جزئیات.

## ۹) نگاره‌ها / Patterns
لیستنگ با فیلتر سبک/رنگ/تگ + مرتب‌سازی + صفحه‌بندی (اسکرول به نتایج) + جستجوی `?q=`. صفحهٔ جزئیات نگاره: پیش‌نمایش بزرگ PatternArt، خالق، سبک/دسته/پالت رنگ با hex، کاربردها، محصولات کاربرِ آن نگاره، نگاره‌های مرتبط، باکس لایسنس + CTA (عمداً به `/b2b` چون لایسنس محصول سبد نیست — افشا شده)، JSON-LD نوع CreativeWork.

## ۱۰) محصولات / Products & detail
لیستنگ با ProductBrowser (جستجو، چیپ دسته، سورت، صفحه‌بندی). جزئیات محصول: گالری چنداسلایدی با بندانگشت و نشان‌ها، نام/SKU/قیمت/واریانت/تعداد/موجودی، افزودن به سبد و Buy Now → `/checkout`، جدول مشخصات، توصیف، ارسال، محصولات مرتبط، نگارهٔ مرتبط، خالق، JSON-LD نوع Product (+Offer/Rating).

## ۱۱) هنرمندان / Artists & profiles
آرشیو هنرمندان + پروفایل کامل: کاور، آواتار، نام، حرفه، بیو + روایت، آمار، دنبال‌کردن (Follow toggle)، تب‌های نگاره‌ها/محصولات/نمونه‌کارها، JSON-LD نوع Person. آواتارهای واقعی عکس‌دار شدند.

## ۱۲) نمونه‌کار / Portfolio
بخش مهم پروژه: سکشن خانگی + آرشیو ماسونری (کارت‌های lg/md متناوب) + جزئیات با هیرو، داستان، گالری، نگاره‌ها/محصولات به‌کاررفته، خالق، موارد مرتبط؛ حضور در پروفایل هنرمندان؛ JSON-LD نوع VisualArtwork. عکس‌های واقعی برای لابی‌هتل/کافه/اتاق‌کودک/شوروم.

## ۱۳) آکادمی / Education
صفحهٔ آکادمی (نه وبلاگ): دورهٔ ویژهٔ افقی + دوره‌ها + مسیرهای یادگیری ۴گانه + آموزش‌ها/مقالات؛ جزئیات با کاور، متا (نوع/سطح/مدت)، محتوا، نویسنده، کارت خلاصهٔ CTA، مرتبط‌ها؛ JSON-LD نوع Course/Article. کاورهای واقعی (سوێچ طراحی، بافت‌دستی).

## ۱۴) فروشگاه / Store ecosystem
فروشگاه مستقل کامل: هیرو، دسته‌بندی‌ها با شمارش، ویژه، بنر پرومو، تازه‌ها، کالکشن انحصاری، پرفروش‌ها، پیشنهادی‌ها، خبرنامه؛ `/store/category/[slug]` با فیلتر ثابت دسته + چیپ‌های دسته‌های مرتبط؛ `/collections/[slug]` با واش PatternArt و گرید محصول. جستجو با نام/SKU/دسته/جنس/سبک/رنگ/موجودی + سورت/صفحه‌بندی.

## ۱۵) سبد و تسویه / Cart & Checkout
سبد: آیتم‌های خطی (عکس/نام/SKU/مشخصات/تعداد/قیمت/جمع خطی)، حذف و «ذخیره برای بعد» با بازگشت به سبد، نوار پیشرفت ارسال رایگان، کد تخفیف (UI)، خلاصهٔ سفارش، CTA تسویه، لینک ادامهٔ خرید. تسویهٔ تبدیل‌محور در ۴ گام (تماس/نشانی/ارسال با کارت‌های رادیویی/پرداخت) با اعتبارسنجی، خلاصهٔ چسبان با آیتم‌ها و هزینهٔ ارسال داینامیک و صفحهٔ تأیید سفارش با کد رهگیری — دمو، بدون درگاه واقعی (افشا شده). سبد/علاقه‌مندی در `localStorage` مانا است.

## ۱۶) صفحات مؤسساتی / About · Contact · B2B · 404
درباره (ارزش‌ها/آمار)، تماس (کارت‌های اطلاعات + فرم با اعتبارسنجی و وضعیت موفقیت)، B2B (خدمات ۴گانه، فرایند ۴ مرحله‌ای، کیس‌استادی از نمونه‌کار، پیشنهاد فروشگاه، فرم درخواست همکاری)؛ هر فرم صرفاً دموست. 404 دوباینرژیک: نسخهٔ لوکال‌محور (تشخیص زبان از pathname) + ریشه‌ای دوزبانه.

## ۱۷) تصاویر / Imagery
همهٔ ۳۰ مسیر تصویری ارجاعی حالا لوکال و ۲۰۰ هستند: هیرو + ۱۶ محصول + ۶ هنرمند + ۴ نمونه‌کار + ۲ آموزش + OG پیش‌فرض (۱۲۰۰×۶۳۰ مشتق از هیرو). منابع: ۱ تصویر تولیدی AI (هیرو) + استاک وب (Pexels/Unsplash عمداً آزاد) — **افشا:** برای پروداکشن باید مجوزها بازبینی/جایگزین شوند؛ تصاویر با متن/واترمارک رد و مجدد جستجو شدند. کنتراست ابعاد: محصول ۱۴۰۸×۷۶۸، آواتار ۸۰۰×۸۰۰، نمونه‌کار ۱۶۰۰×۱۲۰۰.

## ۱۸) حرکت، دسترس‌پذیری / Motion & accessibility
سیستم موشن متمرکز: Reveal (blur-in/fade-up/slide-up/scale-fade)، stagger، زوم تصویر، اسپات‌لایت کارت، مارکی؛ کاهش‌جنبش رعصت می‌شود. a11y: ناوبری کیبورد (Esc/بازگشت فوکوس در مودال و دراور)، استیت فوکوس مرئی، کنتراست ‎≥ WCAG AA، HTML معنایی (nav/dl/main/figure/aside)، ARIA (`aria-modal`, `aria-expanded`, `aria-live`, لیبل‌های فرم)، اسکیپ‌لینک «پرش به محتوا»، alt مناسب.

## ۱۹) عملکرد / Performance
همهٔ صفحات SSG، First-Load مشترک **۱۰۳ kB**، بدون کتابخانهٔ انیمیشن سنگین، تصاویر lazy + استاندارد ابعادی/JPEG بهینه، فونت‌های woff2 لوکال، بدون رندر سرورِ داینامیک. بیلد: `next build` سبز با type-checking کامل. در جریان پایدارسازی، خطاهای زیر ریشه‌یابی و رفع شد: alias `tsconfig`، JSX در seo.ts (`seo.tsx`)، تایپ paramsِ Promise محور Next 15، ارجاع `params` در not-found (خطای اصلی prerender)، `generateStaticParams` برای برگ‌ها، تایپ‌های SectionHeader/PatternArt/Reveal.

## ۲۰) سئو / SEO
تمپلیت عنوان «… — نقش»، توضیحات، canonical + `hreflang` دوجهته برای تمام صفحات، OG/Twitter کامل + og-default بومی، `robots.ts` (مسدودسازی cart/checkout) و `sitemap.xml` (هر ۱۴۵ URL با lastmod/priority) هر دو با وضعیت ۲۰۰ تست شدند؛ JSON-LD: Organization سراسری + Product/CreativeWork/Person/VisualArtwork/Course-Article در صفحات جزئیات.

## ۲۱) آمادگی ادمین و موارد باقی‌مانده / Admin-readiness & open items
- **هیچ محتوای قابل‌مدیریت ادمین هاردکد نیست**: سکشن‌ها از داده‌ها، SKUها، دسته‌ها، فیچردها، بنرها، SEO آبجکتی، محصول با مدل کامل (id/sku/name/desc/category/price/salePrice/images/variants/specs/material/dimensions/colors/…). تعویض = اتصال لایهٔ `lib/data` به API/CMS.
- جستجو/سبد/علاقه‌مندی/احراز هویت/بازار: به‌صورت دموی سمت‌کاربر کامل و سالم‌نگه‌داشته‌شده (محلی، بدون بک‌اند واقعی) — افشا شده.
- موارد باز و صادقانه: ۱) صفحهٔ اختصاصی علاقه‌مندی ساخته نشده (تناکم در استیت). ۲) CTA «دریافت لایسنس» به B2B می‌رود (تصمیم طراحی). ۳) تصاویر استاک برای پروداکشن نیازمند مجوز نهایی‌اند. ۴) فونت fa-beirut جایگزین موقت (نکتهٔ ۴). ۵) فرم‌ها و پرداخت صرفاً شبیه‌سازی‌اند.

---

### Checklist تأیید | Verification log
- `next build` → ✓ 145 routes, type-check ok, First Load JS 103 kB
- Route sweep (۲۲ آدرس کلیدی + sitemap/robots) → همه ۲۰۰
- Image asset sweep (۳۰ فایل) → همه ۲۰۰ (با ری‌استارت سرور پس از افزودن دایرکتوری‌های تازه)
- `<html lang="fa" dir="rtl">` / `<html lang="en" dir="ltr">` ✓ — OG/JSON-LD/hreflang ✓

---

## پیوست — باندل حرفه‌ای نهایی (۴ فاز) | Professional Bundle Addendum

### فاز ۱ · پولیش سطح جهانی ✅
- حجم `public/`: ۹٫۹ → ۷٫۳ مگابایت (فشرده‌سازی و جایگزینی تصاویر سنگین)
- ۳ preload قل در `<body>` — حذف CLS/FOUT اصلی
- گزارش کامل: `POLISH-AUDIT.md`

### فاز ۲ · پنل مدیریت محتوا ✅
- مسیر: `/fa/admin` (noindex، فقط فارسی) — وضعیت: ۲۰۰ ✓
- ویرایش: کپی هیرو (fa/en)، متن marquee، بنرهای هیروی ۹ صفحه
- ذخیره: localStorage + خروجی/ورودی JSON ← کامیت در `content/cms/site-content.json`
- مسیر ارتقا به Sanity/Strapi با نقطهٔ اتصال واحد `src/lib/i18n.ts`
- مستندات: `README-CMS.md`

### فاز ۳ · پکیج هویت بصری ✅ — `brand-kit/`
- `BRAND-BOOK.html` (خودکفا، پیش‌نمایش تعبیه‌شدهٔ همهٔ قطعات)
- کارت‌ویزیت front/back · تگ قیمت (با پانچ) · سربرگ A4 · استیکر گرد — SVG چاپ‌آماده (CMYK-friendly، متن جایایند)
- پالت: INK `#2E4A5C` · GOLD `#C0973F` · IVORY `#F9F6F1` · COPPER `#A05D24`

### فاز ۴ · آماده‌سازی دیپلوی ✅
- `DEPLOY-CHECKLIST.md` — Vercel، DNS، سئو، Basic-Auth ادمین، چک‌لیست لایسنس
- `.env.example` — تمام متغیرهای محیطی حال/آینده

### وضعیت بیلد
- `next build` → ۱۴۶ مسیر (شامل `● /[lang]/admin → /fa/admin`) سبز
- پیش‌نمایش عمومی (Cloudflare tunnel): سایت + ادمین لایو

### فاز ۵ · سیستم 8D تصاویر ⏸️ (غیرفعال به درخواست کاربر — زیرساخت opt-in باقی؛ راهنما: README-FX8D.md)
- لایو: `FXImg` روی ۱۸ سطح (همهٔ صفحات/کارت‌ها/گالری‌ها) — tilt ±۱۰° · پارالاکس اسکرول · برق دنبال‌کنندهٔ نشانگر · جاروی نور · زوم ۱٫۰۸ (حالت دراماتیک، با احترام به reduced-motion/لمسی/تصاویر کوچک)
- فایل: گرید برند sharp روی ۴۵ تصویر (recomb نیلی/برنجی + کنتراست + گرین + وینت + شارپ) · اورجینال‌ها در `assets-originals/` · مانیفست idempotent
- آپلودهای آینده: `npm run grade` خودکار گرید می‌کند؛ FX لایو چون روی کامپوننت سراسری است خودکار اعمال می‌شود
